CREATE TABLE produse (
                         id SERIAL PRIMARY KEY,
                         nume VARCHAR(100) NOT NULL,
                         descriere VARCHAR(100),
                         cantitate INT
);